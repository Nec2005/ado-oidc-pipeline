trigger:
  branches:
    include:
      - main
      - development
  paths:
    include:
      - terraform/*
      - azure-pipelines.yaml

pr:
  branches:
    include:
      - development
      - main
  paths:
    include:
      - terraform/*
      - azure-pipelines.yaml

pool:
  vmImage: 'ubuntu-latest'

variables:
  - name: AWS_REGION
    value: 'eu-west-1'
  - name: SERVICE_CONNECTION_NAME
    value: 'AWS-necatidev-533267146698'
  - name: TERRAFORM_VERSION
    value: '1.13.4'
  - name: TF_PLUGIN_CACHE_DIR
    value: $(Pipeline.Workspace)/.terraform.d/plugin-cache

stages:
  - stage: Plan
    displayName: 'Terraform Plan'
    jobs:
      - job: TerraformPlan
        displayName: 'Run Terraform Plan'
        steps:
          - checkout: self

          # Create the cache directory so the Cache task doesn't fail
          - script: mkdir -p $(TF_PLUGIN_CACHE_DIR)
            displayName: 'Create Plugin Cache Dir'

          - task: Cache@2
            displayName: 'Cache Terraform Providers'
            inputs:
              key: 'terraform | "linux" | terraform/.terraform.lock.hcl'
              restoreKeys: |
                terraform | "linux"
              path: $(TF_PLUGIN_CACHE_DIR)

          - task: TerraformInstaller@1
            inputs:
              terraformVersion: $(TERRAFORM_VERSION)

          # - task: AzureCLI@2
          #   displayName: 'Terraform Plan'
          #   inputs:
          #     azureSubscription: $(SERVICE_CONNECTION_ID)
          #     scriptType: bash
          #     scriptLocation: "inlineScript"
          #     # addSpnToEnvironment adds the ID and Secret (if any) to the environment
          #     addSpnToEnvironment: true
          #     inlineScript: |
          #       export ARM_CLIENT_ID=$servicePrincipalId
          #       export ARM_TENANT_ID=$tenantId
          #       export ARM_SUBSCRIPTION_ID=$(az account show --query id -o tsv)

          #       terraform init
          #       terraform plan
          #   env:
          #     ARM_USE_OIDC: true
          #     SYSTEM_ACCESSTOKEN: $(System.AccessToken)
          #     SYSTEM_OIDCREQUESTURI: $(System.OidcRequestUri)
          - task: AWSShellScript@1
            displayName: 'Terraform Init & Plan'
            inputs:
              awsCredentials: $(SERVICE_CONNECTION_NAME)
              regionName: $(AWS_REGION)
              scriptType: 'inline'
              inlineScript: |
                set -e
                cd terraform
                terraform init -input=false
                terraform plan -out=tfplan
                terraform show -no-color tfplan

          - task: PublishPipelineArtifact@1
            displayName: 'Publish Plan (Excluding .terraform)'
            inputs:
              targetPath: '$(System.DefaultWorkingDirectory)/terraform'
              artifact: 'terraform-plan'

  - stage: Apply
    displayName: 'Terraform Apply'
    dependsOn: Plan
    # Only runs on Merges (IndividualCI) to development or main branches
    condition: |
      and(
        succeeded(),
        eq(variables['Build.Reason'], 'IndividualCI'),
        or(
          eq(variables['Build.SourceBranch'], 'refs/heads/main'),
          eq(variables['Build.SourceBranch'], 'refs/heads/development')
        )
      )
    jobs:
      - deployment: TerraformApply
        displayName: 'Apply Terraform Changes'
        environment: 'dev'
        strategy:
          runOnce:
            deploy:
              steps:
                - checkout: self

                - script: mkdir -p $(TF_PLUGIN_CACHE_DIR)
                  displayName: 'Create Plugin Cache Dir'

                - task: Cache@2
                  displayName: 'Cache Terraform Providers'
                  inputs:
                    key: 'terraform | "linux" | terraform/.terraform.lock.hcl'
                    restoreKeys: |
                      terraform | "linux"
                    path: $(TF_PLUGIN_CACHE_DIR)

                - task: TerraformInstaller@1
                  inputs:
                    terraformVersion: $(TERRAFORM_VERSION)

                - task: DownloadPipelineArtifact@2
                  displayName: 'Download Plan'
                  inputs:
                    artifact: 'terraform-plan'
                    path: '$(System.DefaultWorkingDirectory)/terraform'

                - task: AWSShellScript@1
                  displayName: 'Terraform Apply'
                  inputs:
                    awsCredentials: $(SERVICE_CONNECTION_NAME)
                    regionName: $(AWS_REGION)
                    scriptType: 'inline'
                    inlineScript: |
                      set -e
                      cd terraform

                      # Re-init uses the cache we restored above
                      terraform init -input=false

                      echo "Applying Terraform plan..."
                      terraform apply -auto-approve tfplan
